# CREATE PLAN RULES

- Generate plan as TMP_PLAN.md file in root project directory. If file already exists, REWRITE the file.
- MUST BE DETAILED, THINK DEEPLY, and CRITICAL so the other coding agent (such as yourself) can execute the plan perfectly. DON'T MISS any steps